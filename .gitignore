# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log

# Node (only if tooling is added later)
node_modules/

# 비밀키 — 절대 커밋 금지 (.env.example 만 커밋합니다)
.env
.env.*
!.env.example

# Firebase 서비스 계정 키 (Admin SDK) — 프로젝트 전체 권한이라 유출되면 치명적
*-firebase-adminsdk-*.json
serviceAccountKey*.json
*service-account*.json
.firebase/
firebase-debug.log

# Vercel
.vercel

# Claude Code (local tooling/config)
.claude/

# Hosting 배포용 생성물 (scripts/build-public.js 가 만듭니다)
public/
.firebase/
